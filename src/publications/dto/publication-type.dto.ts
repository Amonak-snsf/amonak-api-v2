export enum PublicationType {

    post = 'post',
    sale = 'sale',
    alerte = 'alerte',
    publicity = 'publicity',
    share = 'share',
    default = 'default'
}

export enum AlerteType {

    default = 'default',
    urgent = 'urgent'
}

export enum AlerteDuration {

    default = 'short'
}

export enum SaleType {

    default = 'default'
}